{
  "name": "js",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist src/index.html .parcel-cache",
    "dev": "cd src && node genere_index.js > index.html && cd .. && parcel src/index.html",
    "build": "cd src && node genere_index.js > index.html && cd .. && parcel build src/index.html"
  },
  "dependencies": {
    "photoswipe": "^5.3.2"
  },
  "devDependencies": {
    "swig": "^1.4.2",
    "sharp": "^0.31.0",
    "parcel": "^2.7.0"
  }
}
